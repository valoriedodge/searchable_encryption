class Rule:
	def __init__(self):
        self.number = number
        self.tokens = []
        self.to_find = []       

    def add_tokens(self, tokens):
        self.tokens.append(tokens)
        self.to_find.append([])
        for i in range(0,len(first_tokens)):
    		self.to_find[-1].append(i)
    
    def add_second_tokens(self, tokens):
        self.second_tokens = tokens
        for i in range(0,len(second_tokens)):
    		self.second_to_find.append(i)

    def mark_found(self, token):
    	for content in tokens:
    		for x in range(0, len(content))
    			if content[x] = token:


    def check_if_found(self):
    	for content in to_find:
